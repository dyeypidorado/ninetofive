%table
  %tr
    %th Name
    %th Email
    %th Step/ Promotion Name
    %th Referrer
    %th From Facebook

  - @subs.each do |sub|
    %tr
      %td= sub.subscriber.name
      %td= sub.subscriber.email
      %td= sub.step_promotion_name
      %td= sub.affiliate.name unless sub.affiliate.nil?
      %td= sub.is_fb unless sub.is_fb.nil?

