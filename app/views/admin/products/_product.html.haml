- if template == :table_row
  %tr
    %td #{product.name}
    %td #{product.category.name}
    %td= link_to product.page.title, [:admin, product.page]
    %td.actions
      = link_to "Edit", [:edit, :admin, product.category, product]

