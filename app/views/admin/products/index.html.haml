%h1 Welcome to 9to5 Millionaire
%table.pretty
  %tr
    %th Name
    %th Actions
  - for product in @products
    = render product, template: :table_row
%br
= link_to "Add New #{@category.name.singularize}", [:new, :admin, @category, :product]

